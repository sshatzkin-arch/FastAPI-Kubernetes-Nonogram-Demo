apiVersion: v1
kind: Service
metadata:
  name: {{ template "nanogramapi.fullname" . }}
  labels:
    app: {{ template "nanogramapi.name" . }}
    chart: {{ template "nanogramapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ template "nanogramapi.name" . }}
    release: {{ .Release.Name }}
